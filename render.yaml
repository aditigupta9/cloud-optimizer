services:
  - type: web
    name: cloud-optimizer
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app_flask.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: FLASK_DEBUG
        value: "false"
